import React from "react";

type Props = {};

export const Sidebar = (props: Props) => {
  return <nav className="sidebar-transition  fixed overflow-hidden top-0 left-0 h-screen bg-white shadow-md w-[--sidebar-width] ">sidebar</nav>;
};
